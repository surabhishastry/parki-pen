import { createContext } from "react";

// Authentication context to keep track of current user authentication status
export const AuthContext = createContext();

// Alert context to keep track of current alerts
export const AlertContext = createContext();
